import { createAction } from '@ngrx/store';

export const hardAlcohol = createAction('[Night Component] HardAlcohol');
export const beer = createAction('[Night Component] Beer');
export const bored = createAction('[Night Component] ');
